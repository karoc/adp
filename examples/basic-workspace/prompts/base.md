# Base Prompt

You are working inside an ADP runtime overlay.

Keep the real project directory clean. Treat generated files such as `AGENTS.md`, `CLAUDE.md`, `.codex/`, and `.claude/` as runtime-only context.

Priorities:

- Make minimal, well-tested changes.
- Explain risky assumptions before changing shared contracts.
- Keep terminal-first workflows scriptable.
