package adapters

import (
	"github.com/karoc/adp/internal/adapters/api"
)

type Adapter = api.Adapter

type Context = api.Context

type RenderResult = api.RenderResult

type GeneratedFile = api.GeneratedFile

type RuntimeHandle = api.RuntimeHandle

type LaunchSpec = api.LaunchSpec
